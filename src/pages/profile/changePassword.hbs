---
fields:
  - name: "oldPassword"
    label: "Старый пароль"
    type: "password"
    value: "password"
  - name: "newPassword"
    label: "Новый пароль"
    type: "password"
    value: "password"
  - name: "newPassword"
    label: "Повторите новый пароль"
    type: "password"
    value: "newPasswordRepeat"
---
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Профиль</title>
  <link rel="stylesheet" href="./profile.scss">
</head>
<body>
  <div class="profile">
    <div class="profile__nav">
      <a href="/main" class="profile__back" />
    </div>
    <div class="profile__content">
      <form class="profile__form">
        <div class="profile__avatar">
          <input id="avatar" class="profile__avatar" name="avatar" type="file" disabled />
          <label for="avatar"></label>
        </div>
        <h3 class="profile__name">Иван</h3>
        {{#each fields}}
          <label class="profile__field">
            <span>{{label}}</span>
            <input name="{{name}}" type="{{type}}" value="{{value}}" />
          </label>
        {{/each}}
      </form>
      <a href="/profile" class="profile__action-button">Сохранить</a>
    </div>
  </div>
</body>
</html>